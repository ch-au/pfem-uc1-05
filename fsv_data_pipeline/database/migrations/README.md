# Pipeline Migration Notes

This folder mirrors migration context used by the maintained pipeline package.

The canonical migration write-up for the repository still lives in `../../database/migrations/README.md`. Keep that file authoritative for shared schema guidance.

Use this package-local folder only when a pipeline-specific packaging or handover note is required.

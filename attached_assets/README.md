# Attached Assets

This folder is treated as a local capture area for screenshots, pasted text, and other session artifacts.

- It is not a source-of-truth documentation folder.
- New raw captures should stay local and are ignored by git.
- If something here matters long-term, move the distilled result into `docs/` or a proper app asset location.
